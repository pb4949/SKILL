let n = Number(prompt("Enter number: "))
let i
let flag = 0
for (i = 2; i < n; i++)
    if (n % i == 0)
    {
        console.log("Number is not prime")
        flag = 1
        break
    }
if (!flag)
    console.log("Number is prime")
